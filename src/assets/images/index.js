const image = {
    logo: require('~/assets/images/logo.svg').default,
    // require () => module object { defafult : link svg}
    // import => link svg
    // => require().default = import
};
export default image;
